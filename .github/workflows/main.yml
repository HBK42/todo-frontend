name: Frontend CI/CD Pipeline

on:
  push:
    branches:
      - main
      - deploy/production

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install dependencies
        run: |
          cd frontend
          npm install

      - name: Lint code
        run: |
          cd frontend
          npm run lint

      - name: Static Code Analysis with SonarQube
        uses: sonarsource/sonarcloud-github-action@v1
        with:
          sonar-project-key: 'your-sonar-project-key-frontend'
          sonar-token: ${{ secrets.SONAR_TOKEN }}

      - name: Security Scan with Snyk
        run: |
          cd frontend
          npx snyk test

  testing:
    runs-on: ubuntu-latest
    needs: linting
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install dependencies
        run: |
          cd frontend
          npm install

      - name: Run tests
        run: |
          cd frontend
          npm test

      - name: Output code coverage
        run: |
          cd frontend
          npm run coverage

  build:
    runs-on: ubuntu-latest
    needs: testing
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: |
          cd frontend
          docker build -t yourusername/todo-frontend:latest .

  deliver:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image to Docker Hub
        run: |
          docker push yourusername/todo-frontend:latest

  deploy:
    runs-on: ubuntu-latest
    needs: deliver
    steps:
      - name: Deploy to AWS
        run: |
          ssh -i ${{ secrets.EC2_SSH_KEY }} ec2-user@your-ec2-instance \
          'docker-compose -f /path/to/docker-compose.yml up -d'
